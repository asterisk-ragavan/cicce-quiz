# Quiz Application Requirements
# Optimized for Python 3.13+ and Windows 10+

# Web Framework
Flask>=3.0.0
Flask-Session>=0.8.0
Werkzeug>=3.0.0

# Template Engine (included with Flask, but pinned for compatibility)
Jinja2>=3.1.0
MarkupSafe>=2.1.0

# Security & Utils
itsdangerous>=2.1.0
click>=8.1.0

# Session Storage
cachelib>=0.12.0

# Database
# SQLite is built-in with Python, no extra package needed

# Build Tools
pyinstaller>=6.0.0

# Optional: For better async support (uncomment if needed)
asgiref>=3.7.0
